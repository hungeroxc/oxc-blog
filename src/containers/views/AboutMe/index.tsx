import React from 'react'

import styles from './index.scss'

const AboutMe = () => {
    return (
        <div className={styles.aboutMe}>
            <div>测试部署效果,jenkins同时部署多个任务直接崩溃</div>
        </div>
    )
}

export default AboutMe
